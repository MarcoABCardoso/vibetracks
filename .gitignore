# Rendered artifacts (WAV/PNG) + manifests are build outputs — regenerate with
# `python -m vibetracks render-all` / `python -m pixeltracks render-all`.
out/

__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/
.venv/
